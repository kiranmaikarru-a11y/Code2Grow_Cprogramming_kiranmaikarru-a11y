kiranmai.k  ,AP25110090185,  day20-smart warehouse analytics system
The program stores product details (ID, quantity, price) in arrays and keeps track of how many products are added
Each menu option is handled using a separate function, as required in the instructions.
Add Product checks for duplicate IDs and ensures quantity and price are positive before saving.
Search and Update functions find the product using its ID and then display or modify its quantity.
Rack Report reads a 3×3 matrix, finds maximum, minimum, and total items using nested loops.
Stock Value and Display functions calculate value = quantity × price and show highest, lowest, and all product details in table format.
