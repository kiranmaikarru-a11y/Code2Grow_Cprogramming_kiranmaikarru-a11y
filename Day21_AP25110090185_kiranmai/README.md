kiranmai.k, AP25110090185, day21-smart city traffic monitoring &violation detection system
ake input of VehicleID, Speed, and Lane into 1D arrays and validate each value.
Check each vehicle for speed violation (>80) and lane violation (VehicleID % 4 ≠ lane).
Count violations, find highest/lowest speed, severity, and traffic density per lane.
Finally display a full report showing all vehicles with their violations and overall statistics.
Separate functions are used to keep the code organized and make the operations reusable.
A summary section displays total violations and traffic density after analyzing all vehicles.
Finally, the program prints a clean table showing each vehicle’s data along with violation indicators.
