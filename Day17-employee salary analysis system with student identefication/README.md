kiranmai.k          AP25110090185          DAY17-Employee Salary Analysis System With Student Identification
    Student Information Input Logic
Use character arrays to store:Student Name    ,Registration Number       ,Section
Employee Salary Input Logic
Use a 1D array to store salaries of employees (up to 10).
Take the number of employees as input (≤ 10).
Then use a loop to read each salary:
Use a loop (while or do-while) to repeatedly show the menu until the user selects “Exit”.
Use if-else statements (since switch is not used) to perform different tasks.
Simply loop through the array and print each salary:
Use a loop to calculate the sum of all salaries.
Initialize both to the first element, then compare in a loop:
Count Above and Below Average Logic
After finding the average:
Exit Logic
When the user enters option 6, break out of the loop.
Display Student Info at End
After all operations or at the end of the program:     
